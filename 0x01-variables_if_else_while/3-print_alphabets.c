#include <stdio.h>
/**
 *main - Entry point of the program.
 *
 *the program prints alphabets lowercase.
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	int n = 97;
	int n = 65;

	while (n <= 122)
	{
		putchar(n);
		n++;
	}
	while (m <= 90)
	{
		putchar(m);
		m++;
	}
	putchar ('\n');
	return (0);
}
